version: '3.8'

services:
  asx-app:
    build: 
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8501:8501"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./backend/data:/app/data
    environment:
      - LM_STUDIO_URL=http://host.docker.internal:1234
    command: streamlit run app.py

  # Frontend (optional - can be disabled if using Streamlit only)
  # frontend:
  #   build:
  #     context: .
  #     dockerfile: frontend/Dockerfile
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - asx-app
